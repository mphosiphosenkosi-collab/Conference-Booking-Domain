# Assignment 1.3 - Requirements Verification

## ✅ 1. Guard Clauses & Defensive Logic
- **Prevent invalid booking requests**: Added guard clauses in CreateBooking(), AddConferenceRoom()
- **Prevent operations on empty collections**: Added Any() checks in GetAvailableRooms(), GetUpcomingBookings()
- **Prevent invalid state transitions**: Throw InvalidOperationException in CancelBooking()

## ✅ 2. Exception Handling
- **When to throw exceptions**: Throw in guard clauses and business rule violations
- **When to let exceptions bubble up**: Let file I/O exceptions bubble unless recoverable
- **Meaningful exception messages**: All exceptions include context-specific messages
- **At least one custom domain-named exception**: Created 5 custom exceptions

## ✅ 3. Edge Cases in Collections & LINQ
- **Empty collections**: Safe handling with Any() checks and empty list returns
- **Missing data**: Null checks with ?. operator and null-coalescing ??
- **Failed queries**: Safe LINQ with FirstOrDefault() instead of First()
- **Avoid unsafe LINQ**: No more First() without checks, all queries are safe

## ✅ 4. Asynchronous File Operations
- **Save booking data to file asynchronously**: SaveDataToFileAsync() method
- **Load booking data from file asynchronously**: LoadDataFromFileAsync() method
- **Use async/await correctly**: Proper async/await pattern throughout
- **Handle I/O failures safely**: Try-catch with specific exception handling

## ✅ 5. Console Application Demonstrations
- **Valid booking scenarios**: Normal booking flow in CreateNewBookingAsync()
- **Invalid booking scenarios**: Guard clause demos show invalid requests
- **Failure handling**: Exception demos show proper error handling
- **Successful and failed async file operations**: Async file operation demos

## Implementation Details:
- **Files Added**: 7 new files (5 exceptions, 2 data services)
- **Files Modified**: 2 files (BookingService.cs, Program.cs)
- **Files Removed**: 1 file (Class1.cs)
- **Total Commits**: 6 logical commits showing incremental implementation
