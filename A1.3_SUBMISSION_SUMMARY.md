# Assignment 1.3 - Robustness, Failures & Asynchronous Operations

## ✅ COMPLETED REQUIREMENTS

### 1. Guard Clauses & Defensive Logic
- **Prevent invalid booking requests**: Guard clauses in CreateBooking(), AddConferenceRoom()
- **Prevent operations on empty collections**: Any() checks in GetAvailableRooms(), GetUpcomingBookings()
- **Prevent invalid state transitions**: Throw InvalidOperationException in CancelBooking()

### 2. Exception Handling
- **When to throw exceptions**: In guard clauses and business rule violations
- **When to let exceptions bubble up**: File I/O exceptions bubble unless recoverable
- **Meaningful exception messages**: All exceptions include context-specific messages
- **Custom domain-named exceptions**: 5 custom exceptions created

### 3. Edge Cases in Collections & LINQ
- **Empty collections**: Safe handling with Any() checks and empty list returns
- **Missing data**: Null checks with ?. operator and null-coalescing ??
- **Failed queries**: Safe LINQ with FirstOrDefault() instead of First()
- **Avoid unsafe LINQ**: No more First() without checks

### 4. Asynchronous File Operations
- **Save booking data asynchronously**: SaveDataToFileAsync() method
- **Load booking data asynchronously**: LoadDataFromFileAsync() method
- **Use async/await correctly**: Proper async/await pattern throughout
- **Handle I/O failures safely**: Try-catch with specific exception handling

### 5. Console Application Demonstrations
- **Valid booking scenarios**: Normal booking flow in CreateNewBookingAsync()
- **Invalid booking scenarios**: Guard clause demos show invalid requests
- **Failure handling**: Exception demos show proper error handling
- **Successful and failed async file operations**: Async file operation demos

## ���️ ARCHITECTURE

### New Files Added:
ConferenceRoomBooking.Services/Exceptions/
├── BookingConflictException.cs
├── InvalidBookingException.cs
├── RoomNotFoundException.cs
├── BookingNotFoundException.cs
└── DataAccessException.cs

ConferenceRoomBooking.Services/Services/
├── IDataService.cs (interface)
└── JsonDataService.cs (implementation)

ConferenceRoomBooking.Services/Models/
└── BookingData.cs (DTO for serialization)

text

### Key Design Patterns:
1. **Defensive Programming**: Guard clauses at method entry points
2. **Custom Exceptions**: Domain-specific exceptions for better error handling
3. **Async/Await Pattern**: Proper async file operations without blocking
4. **Dependency Injection**: IDataService injected into BookingService
5. **Separation of Concerns**: Data access logic separated from business logic

## ��� HOW TO TEST

1. **Run the program**: `dotnet run --project ./src/ConferenceRoomBooking.ConsoleApp`
2. **Test normal operations**: Options 1-6 for regular functionality
3. **Run A1.3 demos**: Option 8 for comprehensive demonstration of all requirements
4. **Test async file ops**: Use Option 7 to save, restart program to load

## ��� COMMIT HISTORY
- Custom exceptions foundation
- Guard clauses & defensive logic
- Safe LINQ & collection handling
- Async file operations
- Console app demos & integration
- Final fixes & testing

## ��� VERIFICATION
- All code compiles without errors or warnings
- All A1.3 requirements implemented and demonstrated
- System persists data across sessions
- Error handling prevents crashes
